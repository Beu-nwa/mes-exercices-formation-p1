// les tableaux associatifs

// declaration d'un tableau associatif

var personne = {
    nom : "Dupon",
    prenom : "Jean",
    age : 25,
};

// affichage d'un tableau associatif
console.table(personne);

// affichage de la valeur a une clé
console.log(personne.prenom);
console.log(personne["nom"]);






























